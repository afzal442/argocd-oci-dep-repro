apiVersion: v2
name: oci-dep-repro
description: Dummy chart in git whose Chart.yaml declares a public oci:// dependency.
type: application
version: 0.1.0
appVersion: "0.1.0"
dependencies:
  - name: nginx
    version: "25.1.9"
    repository: oci://registry-1.docker.io/bitnamicharts
    condition: nginx.enabled
